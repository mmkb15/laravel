@extends('store.layout') @section('title','Wishlist') @section('content')
<h2>Wishlist</h2><div class="row g-4 mt-1">@forelse($wishlists as $w)@php($p=$w->product)@php($s=$p->skus->first())<div class="col-6 col-md-3"><div class="card h-100"><div class="card-body"><h5>{{$p->name}}</h5><div>৳ {{$s?->price}}</div><a href="{{route('products.show',$p)}}" class="btn btn-dark mt-3 w-100">View</a><form method="POST" action="{{route('wishlist.toggle',$p)}}">@csrf<button class="btn btn-link text-danger w-100">Remove</button></form></div></div></div>@empty<div class="alert alert-info">Wishlist is empty.</div>@endforelse</div>{{$wishlists->links()}}@endsection
