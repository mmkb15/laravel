@extends('store.layout') @section('title','Mursalin Store') @section('content')
<div class="hero p-5 mb-5"><div class="row align-items-center"><div class="col-lg-7"><h1 class="display-5 fw-bold">Everything you need, in one store.</h1><p class="lead">Browse products, save favorites, and checkout securely.</p><a href="{{route('products')}}" class="btn btn-light btn-lg">Shop now</a></div></div></div><h3 class="mb-3">Categories</h3><div class="row g-3 mb-5">@foreach($categories as $c)<div class="col-6 col-md-3"><a class="card text-decoration-none text-dark h-100" href="{{route('products',['category'=>$c->category_id])}}"><div class="card-body"><h5>{{$c->name}}</h5><small>Browse products</small></div></a></div>@endforeach</div><div class="d-flex justify-content-between mb-3"><h3>Latest Products</h3><a href="{{route('products')}}">View all</a></div><div class="row g-4">@foreach($products as $p)@php($s=$p->skus->first())<div class="col-6 col-md-4 col-lg-3"><div class="card product-card h-100">@if($s?->image_url)<img src="{{Storage::url($s->image_url)}}" class="card-img-top">@else<div class="bg-light d-flex align-items-center justify-content-center" style="height:230px">No image</div>@endif<div class="card-body"><small class="text-muted">{{$p->category->name}}</small><h5 class="card-title mt-1">{{$p->name}}</h5><div class="price">৳ {{number_format($s?->price??0,2)}}</div><a href="{{route('products.show',$p)}}" class="btn btn-dark w-100 mt-3">View product</a></div></div></div>@endforeach</div>
@endsection
