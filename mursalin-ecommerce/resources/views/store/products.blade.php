@extends('store.layout') @section('title','Products') @section('content')
<div class="row g-4"><aside class="col-lg-3"><div class="card"><div class="card-body"><h5>Filters</h5><form><input type="hidden" name="q" value="{{$q}}"><label class="form-label">Category</label><select class="form-select mb-3" name="category"><option value="">All</option>@foreach($categories as $c)<option value="{{$c->category_id}}" @selected($cat==$c->category_id)>{{$c->name}}</option>@endforeach</select><label class="form-label">Brand</label><select class="form-select mb-3" name="brand"><option value="">All</option>@foreach($brands as $b)<option value="{{$b->brand_id}}" @selected($brand==$b->brand_id)>{{$b->name}}</option>@endforeach</select><button class="btn btn-dark w-100">Apply</button></form></div></div></aside><section class="col-lg-9"><div class="d-flex justify-content-between mb-3"><h3>Products</h3><span>{{$products->total()}} results</span></div><div class="row g-4">@foreach($products as $p)@php($s=$p->skus->first())<div class="col-6 col-md-4"><div class="card product-card h-100">@if($s?->image_url)<img src="{{Storage::url($s->image_url)}}" class="card-img-top">@else<div class="bg-light d-flex align-items-center justify-content-center" style="height:230px">No image</div>@endif<div class="card-body"><small>{{$p->brand?->name}}</small><h5>{{$p->name}}</h5><div class="price">৳ {{number_format($s?->price??0,2)}}</div><a href="{{route('products.show',$p)}}" class="btn btn-outline-dark w-100 mt-2">View</a></div></div></div>@endforeach</div><div class="mt-4">{{$products->links()}}</div></section></div>@endsection
