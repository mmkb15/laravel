@extends('admin.layouts.app') @section('title',$category->exists?'Edit Category':'Add Category') @section('content')
<div class="tf-section-2"><h4>{{$category->exists?'Edit Category':'Add Category'}}</h4></div><div class="wg-box mt-3"><form method="POST" action="{{$category->exists?route('admin.categories.update',$category):route('admin.categories.store')}}">@csrf @if($category->exists)@method('PUT')@endif
<div class="row g-3"><div class="col-md-6"><label>Name</label><input class="form-control" name="name" value="{{old('name',$category->name)}}" required></div><div class="col-md-6"><label>Slug</label><input class="form-control" name="slug" value="{{old('slug',$category->slug)}}"></div><div class="col-md-6"><label>Parent</label><select class="form-select" name="parent_id"><option value="">Root</option>@foreach($parents as $p)<option value="{{$p->category_id}}" @selected(old('parent_id',$category->parent_id)==$p->category_id)>{{$p->name}}</option>@endforeach</select></div><div class="col-12"><label>Description</label><textarea class="form-control" name="description" rows="4">{{old('description',$category->description)}}</textarea></div></div><button class="tf-button style-1 mt-4">Save</button> <a href="{{route('admin.categories.index')}}" class="tf-button style-3 mt-4">Cancel</a></form></div>@endsection
